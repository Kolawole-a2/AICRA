# Praxis Defense: Imbalanced Data Handling - Complete Summary

## ✅ All Techniques Verified and Implemented

This document provides a quick reference for your praxis defense regarding imbalanced data handling techniques.

---

## 1. Focal Loss (α > 0.5, γ ≈ 2) ✅

**Status:** ✅ **VERIFIED**Proof Locations:- **Implementation:** `aicra/pipelines/training.py`, lines 212-233
- **Usage:** `aicra/pipelines/training.py`, line 162
- **Alternative:** `aicra/utils/train_lightgbm.py`, lines 72-78

**Parameters:- α = 0.75 (> 0.5 ✅)
- γ = 2.0 (≈ 2 ✅)

**Evidence:```python
# aicra/pipelines/training.py:162
criterion = FocalLoss(alpha=0.75, gamma=2.0)
```

---

## 2. Class-Balanced Loss ✅

**Status:** ✅ **VERIFIED**Proof Locations:- **Implementation:** `aicra/pipelines/training.py`, line 113
- **Configuration:** `aicra/config.py`, line 56

**Evidence:```python
# aicra/config.py:56
class_weight: str | None = "balanced"

# aicra/pipelines/training.py:113
class_weight=self.settings.class_weight  # "balanced"
```

---

## 3. Class Weighting ✅

**Status:** ✅ **VERIFIED**Proof Locations:- **Configuration:** `aicra/config.py`, line 56
- **Usage:** `aicra/pipelines/training.py`, line 113
- **Computation:** `aicra/pipelines/training.py:128-136` (`_compute_scale_pos_weight`)

**Evidence:- Default `class_weight="balanced"` in configuration
- Automatically computes `scale_pos_weight = n_neg / n_pos` when balanced

---

## 4. Stratified AND Time-Ordered Splits ✅

**Status:** ✅ **VERIFIED** (Enhanced)

**Proof Locations:- **Implementation:** `aicra/utils/data_loader.py`, lines 65-108
- **Function:** `load_ember_2024(time_ordered=True, stratified=True)`

**Evidence:- ✅ Stratified split: Lines 109-118
- ✅ Time-ordered split: Lines 65-70
- ✅ **Combined split:** Lines 72-108 (time-windowed stratified sampling)

**Usage:```python
from aicra.utils.data_loader import load_ember_2024

# Combined stratified + time-ordered split
train, val, test = load_ember_2024(
    return_val=True,
    time_ordered=True,  # Temporal ordering
    stratified=True,     # Class distribution preservation
    val_split=0.1
)
```

**How it works:1. Sorts data by timestamp (time ordering)
2. Performs stratified sampling within time windows
3. Ensures all training timestamps < validation timestamps
4. Preserves class distribution as much as possible

---

## 5. Cost-Sensitive Thresholding (FN≫FP) ✅

**Status:** ✅ **VERIFIED**Proof Locations:- **Core Function:** `aicra/core/evaluation.py`, lines 68-84
- **Configuration:** `aicra/config.py`, lines 65-66
- **Usage:** `aicra/experiments/h1_classification.py`, lines 648-651

**Cost Ratios:- **Default Config:** FN:FP = 100:1 ✅
- **H1 Experiment:** FN:FP = 100:1 ✅
- **H2 Experiment:** FN:FP = 10:1 ✅

**Evidence:```python
# aicra/core/evaluation.py:68-84
def cost_sensitive_threshold(
    y_true: np.ndarray,
    y_prob: np.ndarray,
    cost_fn: float,  # False Negative cost
    cost_fp: float,  # False Positive cost
) -> float:
    # Minimizes: fn * cost_fn + fp * cost_fp
    ...

# aicra/experiments/h1_classification.py:648-651
banking_cost_fn = 100.0  # High cost for false negatives
banking_cost_fp = 1.0    # Low cost for false positives
banking_threshold = cost_sensitive_threshold(
    y_true_test, y_prob_test, cost_fn=banking_cost_fn, cost_fp=banking_cost_fp
)
# Ratio: 100:1 (FN≫FP ✅)
```

---

## Validation Reports Generated

1. **`IMBALANCED_DATA_HANDLING_VALIDATION.md`** - Comprehensive validation guide with all proof locations
2. **`IMBALANCED_DATA_HANDLING_PROOF_REPORT.md`** - Automated validation report (generated by script)
3. **`imbalanced_handling_validation_results.json`** - Machine-readable validation results
4. **`validate_imbalanced_handling.py`** - Validation script (run anytime to verify)

---

## Quick Verification Commands

```bash
# Run automated validation
python validate_imbalanced_handling.py

# Check Focal Loss
grep -n "FocalLoss\|alpha=0.75\|gamma=2.0" aicra/pipelines/training.py

# Check Class Weighting
grep -n "class_weight" aicra/config.py aicra/pipelines/training.py

# Check Splits
grep -n "stratified\|time_ordered" aicra/utils/data_loader.py

# Check Cost-Sensitive Thresholding
grep -n "cost_sensitive_threshold\|cost_fn\|cost_fp" aicra/core/evaluation.py aicra/config.py
```

---

## Summary Table

| Technique | Status | Key File | Line Numbers | Parameters |
|-----------|--------|----------|--------------|------------|
| **Focal Loss** | ✅ | `aicra/pipelines/training.py` | 212-233, 162 | α=0.75, γ=2.0 |
| **Class-Balanced Loss** | ✅ | `aicra/pipelines/training.py` | 113 | `class_weight="balanced"` |
| **Class Weighting** | ✅ | `aicra/config.py` | 56 | `class_weight="balanced"` |
| **Stratified Splits** | ✅ | `aicra/utils/data_loader.py` | 109-118 | `stratified=True` |
| **Time-Ordered Splits** | ✅ | `aicra/utils/data_loader.py` | 65-70 | `time_ordered=True` |
| **Combined Splits** | ✅ | `aicra/utils/data_loader.py` | 72-108 | Both flags |
| **Cost-Sensitive Thresholding** | ✅ | `aicra/core/evaluation.py` | 68-84 | FN:FP = 100:1 |

---

## For Your Defense

### What to Say:

1. **Focal Loss:** "We implement Focal Loss with α=0.75 and γ=2.0, as specified in the requirements. This is implemented in our training pipeline for FFNN models."

2. **Class-Balanced Loss:** "We use class-balanced loss through LightGBM's `class_weight='balanced'` parameter, which automatically adjusts for class imbalance."

3. **Class Weighting:** "Class weighting is configured by default in our settings, and we also compute `scale_pos_weight` to further balance the classes."

4. **Stratified AND Time-Ordered Splits:** "We support both stratified and time-ordered splits, and have enhanced our data loader to support both simultaneously using time-windowed stratified sampling, ensuring both temporal ordering and class distribution preservation."

5. **Cost-Sensitive Thresholding:** "We implement cost-sensitive thresholding with a 100:1 ratio favoring false negatives over false positives, aligned with banking security requirements where missing ransomware is far more costly than investigating false positives."

### Evidence Files to Reference:

- `IMBALANCED_DATA_HANDLING_VALIDATION.md` - Complete guide
- `IMBALANCED_DATA_HANDLING_PROOF_REPORT.md` - Automated validation
- `validate_imbalanced_handling.py` - Validation script

--*All techniques verified and ready for praxis defense.* ✅

